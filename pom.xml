<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/>
	</parent>

	<groupId>com.gtalent</groupId>
	<artifactId>redis-dessert</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>redis-dessert</name>
	<description>甜點訂單系統 (Spring Boot 3.x + MySQL + Redis)</description>

	<properties>
		<!-- Spring Boot 4.x 最低要求 Java 17，但 Spring AI 2.0 平台升級到
		     Spring Boot 4 / Spring Framework 7 之後，開發環境需要 Java 21。 -->
		<java.version>21</java.version>
		<lombok.version>1.18.46</lombok.version>
		<logback.version>1.5.25</logback.version>
		<!-- Spring AI 版本相容表：
		     - 1.1.x 分支對應 Spring Boot 3.5.x
		     - 2.x  分支對應 Spring Boot 4.x（GA 已於 2026/6/12 發布）
		     搭配 Spring Boot 4.1.0，這裡改用 2.0.0 GA。
		     注意 2.0.0-M3 起有 Jackson 2 -> Jackson 3 遷移、MCP 套件改名等破壞性變更，
		     若程式碼有直接依賴 Jackson 2 特定 API 或 MCP annotation 套件路徑，需要調整。 -->
		<spring-ai.version>2.0.0</spring-ai.version>
	</properties>

	<!-- 統一用 BOM 管理 Spring AI 各模組版本，避免各 dependency 各自寫版本號、
	     日後升級時漏改造成版本不一致 -->
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.ai</groupId>
				<artifactId>spring-ai-bom</artifactId>
				<version>${spring-ai.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-pool2</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-mongodb</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.datatype</groupId>
			<artifactId>jackson-datatype-jsr310</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>${lombok.version}</version>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>${logback.version}</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-core</artifactId>
			<version>${logback.version}</version>
		</dependency>

		<!-- spring-ai-core 在 1.1.x 已被拆分成 spring-ai-commons / spring-ai-model /
		     spring-ai-client-chat / spring-ai-vector-store 等模組，spring-ai-bom
		     裡已經沒有這個 artifactId 了。不需要手動加，下面的 starter 會自動帶入所需模組。 -->

		<!-- 注意：舊的 artifactId 是 spring-ai-mongodb-atlas-store-spring-boot-starter，
		     1.0.0-M7 之後改名為 spring-ai-starter-vector-store-mongodb-atlas，
		     沿用舊名稱在新版本會直接找不到套件 -->
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-vector-store-mongodb-atlas</artifactId>
		</dependency>

		<!-- 新增：Google GenAI 對話模型（Gemini），走 API key 模式 -->
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-model-google-genai</artifactId>
		</dependency>

		<!-- 新增：Google GenAI 文字嵌入模型，補上原本缺的 EmbeddingModel bean -->
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-model-google-genai-embedding</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-csv</artifactId>
			<version>1.11.0</version>
		</dependency>
	</dependencies>

	<build>
		<finalName>app</finalName>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<mainClass>com.gtalent.redis.dessert.RedisDessertApplication</mainClass>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
				<executions>
					<execution>
						<id>repackage</id>
						<goals>
							<goal>repackage</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<!-- 預設排除標記為 integration 的測試（例如 contextLoads），
			     這類測試需要連接 MySQL/Redis/MongoDB 等外部服務，
			     本機沒有起 docker-compose 時執行 mvn test 會直接失敗。
			     需要真的跑這類測試時，見下方 integration-test profile。 -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<excludedGroups>integration</excludedGroups>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<!-- 需要在外部服務（docker-compose 起的 MySQL/Redis/MongoDB）都就緒時，
	     手動驗證整合測試，執行：mvn test -Pintegration-test -->
	<profiles>
		<profile>
			<id>integration-test</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-surefire-plugin</artifactId>
						<configuration>
							<excludedGroups></excludedGroups>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<!-- Spring AI 2.0.0 已是正式 GA 版本，可直接從 Maven Central 取得，
	     理論上不再需要 spring-milestones / spring-snapshots；
	     保留這段是為了方便未來想搶先試用 milestone 版（如 2.1.0-M1）時直接使用。 -->
	<repositories>
		<repository>
			<id>spring-milestones</id>
			<name>Spring Milestones</name>
			<url>https://repo.spring.io/milestone</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
		<repository>
			<id>spring-snapshots</id>
			<name>Spring Snapshots</name>
			<url>https://repo.spring.io/snapshot</url>
			<releases>
				<enabled>false</enabled>
			</releases>
		</repository>
	</repositories>

</project>